from typing import List
class Solution:
    def maxProfit(self, inventory: List[int], orders: int) -> int:
        mod = 10**9+7
        n = len(inventory)
        inventory.sort(reverse = True)
        cur = inventory[0]
        ans = 0
        colornum = 0
        while orders>0:
            while class Solution:
    def maxProfit(self, inventory: List[int], orders: int) -> int:
        
            if colornum<n and cur == inventory[colornum]:
                colornum+=1
